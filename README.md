# rust-autoclicker
autoclicker written in rust with "macro" functionality, super fast and super simple



## Build

```bash
git clone https://github.com/WinWeirdo/rust-autoclicker
cd rust-autoclicker
cargo build
```

Build the project yourself using Cargo.
